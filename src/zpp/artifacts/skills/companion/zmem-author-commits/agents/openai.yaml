interface:
  display_name: "Author zmem Commits"
  short_description: "Write commits with durable zmem annotations"
  default_prompt: "Use $zmem-author-commits to prepare a clear commit message with appropriate zmem annotations."
