## 1. Public Behavior Gates

- [x] 1.1 Add scenario-selected RED coverage for runtime-owned manifest and owner creation plus strict environment overrides to the OpenSpec Bundler integration feature root; retain identity reuse and conflict boundaries for focused matrices.
- [x] 1.2 Add scenario-selected RED coverage for acknowledged one-command bypass, missing authorization input, visible warning, structured bypass evidence, and non-persistence to the capability-owned bypass feature root.
- [x] 1.3 Classify skill/runtime responsibility as non-observable governance, retain it in OpenSpec, and avoid artifact-wording assertions in consolidated-workflow BDD.

## 2. Runtime Coordination Utilities

- [x] 2.1 Add strict versioned product-home workflow identity loading and exclusive atomic UUID-v4 initialization that survives Bundler reset.
- [x] 2.2 Add strict `ZPP_WORKFLOW_COORDINATION` JSON decoding for optional owner and root-to-store overrides; reject unknown, malformed, empty, ambiguous, or mismatched values before mutation.
- [x] 2.3 Add an OpenSpec registry adapter that resolves a unique registration by root or invokes public JSON registration for an authorized healthy repo-local root.
- [x] 2.4 Add idempotent store-manifest preparation using exclusive creation, canonical UUID-v4 validation, concurrent-winner reread, and no invalid-manifest repair.
- [x] 2.5 Compose exact repository/change targets into one automatic acquisition result containing registration, manifest, owner, store UUID, member, and bundle evidence.

## 3. Lease Bridge and Bypass Wiring

- [x] 3.1 Extend `zpp lease acquire` with runtime-owned repository/change target mode and optional managed owner while preserving explicit owner plus UUID-member mode for diagnostics and recovery.
- [x] 3.2 Let archive recording, completion, and authorized abandonment use the selected home's managed owner when explicit owner input is omitted, rejecting bundles owned by another identity.
- [x] 3.3 Add `zpp bypass --reason TEXT --acknowledge -- COMMAND...` with an ephemeral child-only marker, pre-execution stderr warning, exact command reporting, and child exit-code propagation.
- [x] 3.4 Return structured bypass evidence from governed runtime entry points without granting mutation, archive, checkpoint, verification, or lifecycle authority.

## 4. Workflow Integration

- [x] 4.1 Reauthor `zpps-workflow-kernel` to accept exact roots/change names and consume the runtime's structured coordinated or bypassed result without requiring caller-supplied internal identifiers.
- [x] 4.2 Update complete playbooks and mutating stage/OpenSpec components to call the kernel/runtime boundary, remove store-registration and durable-owner prompts, and preserve concrete conflict blocking.
- [x] 4.3 Update README command guidance for automatic coordination, strict environment overrides, explicit recovery inputs, and visibly warned bypass execution.

## 5. Verification and Specification Formation

- [x] 5.1 Add focused unit matrices for product-home identity, override decoding, registration, manifest preparation, automatic acquisition, managed-owner lifecycle operations, and bypass execution.
- [x] 5.2 Run each changed capability-owned Behave root independently and the focused Python selectors, then run the complete native test surface.
- [x] 5.3 Run supported-interpreter, lock, lint, format, and clean package-build gates through the repository's declared tooling.
- [x] 5.4 Convert executable delta scenarios to exact trace-only anchors, synchronize canonical specifications, strictly validate the change and all canonical specs, and inspect packaged skills for runtime-only coordination ownership.
