## 1. Skill Assets

- [x] 1.1 Run the skill creator initializer in a disposable directory and use its validated structure to author the packaged `zpp-maintain-openspec` companion skill.
- [x] 1.2 Add concise core instructions, matching `agents/openai.yaml`, and the detailed audit, consolidation, and removal contract reference.
- [x] 1.3 Validate the completed skill with the skill creator's `quick_validate.py` and packaged Agent Router loading.

## 2. Executable Inventory Verification

- [x] 2.1 Prove the existing fixed companion inventory expectation RED after the valid skill directory is packaged.
- [x] 2.2 Update only the focused unit inventory expectation for `zpp-maintain-openspec`; add no prose-pinning test or Gherkin.
- [x] 2.3 Run the focused artifact unit tests GREEN and confirm the generic dynamic discovery behavior still covers projection.

## 3. Specification Reconciliation

- [x] 3.1 Synchronize the accepted `openspec-maintenance-skill` delta into canonical specifications without initializing local OpenSpec skills.
- [x] 3.2 Run strict OpenSpec validation and inspect the canonical diff for complete requirement and scenario preservation.

## 4. Final Verification

- [ ] 4.1 Check the lock and supported Python, then run declared lint, format, test, and clean build gates.
- [ ] 4.2 Archive this completed change and verify the incremental checkpoint series and clean working tree.
