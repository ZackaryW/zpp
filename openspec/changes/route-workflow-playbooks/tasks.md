## 1. Public Inventory Regression

- [x] 1.1 Update the established consolidated-workflow installed-inventory scenario and capability support to require `zpp-generic-workflow` in deterministic order while retaining the explicit legacy entry, then prove the focused RED.
- [x] 1.2 Add focused artifact and lifecycle inventory cases for the six-entry public set without asserting arbitrary Markdown wording or fabricating executable routing behavior.

## 2. Workflow Entry Contracts

- [x] 2.1 Add `zpp-generic-workflow` as the complete current generic sequence by moving the existing generic playbook without changing its ordered stages or authority boundaries.
- [x] 2.2 Narrow `zpp-auto` so workflow-shaped mixed, maintenance-oriented, or unspecialized product requests select the generic entry, accepted direct artifact maintenance remains separate, and genuine non-matches return without handoff.
- [x] 2.3 Reduce `zpp-legacy-workflow` to an explicit one-hop compatibility entry that preserves the request and supplied authority, invokes the generic entry once, and owns no lifecycle stages or continuation.

## 3. Projection and Lifecycle

- [x] 3.1 Add the generic entry to the deterministic packaged workflow family and update exact inventory validation and projection ordering.
- [x] 3.2 Verify initialization, synchronization, grouped workflow lifecycle, and reset consume the shared six-entry inventory while preserving ownership-safe obsolete handling.

## 4. Contract and Repository Verification

- [x] 4.1 Form the spec-only routing obligations and the installed-inventory trace, then synchronize the consolidated-workflow delta into the canonical specification.
- [x] 4.2 Run focused and complete Behave and pytest surfaces, lint, format checking, lock validation, build, supported-interpreter evidence, and strict OpenSpec validation.

## 5. Release Version Synchronization

- [x] 5.1 Set the project, public module, and locked editable-distribution versions to `2.1.4` without changing dependency resolution.
- [ ] 5.2 Verify project, module, CLI, and built-distribution version agreement, then rerun the complete repository release gates.
