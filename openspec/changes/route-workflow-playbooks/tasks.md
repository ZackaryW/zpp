## 1. Public Inventory Regression

- [ ] 1.1 Update the established consolidated-workflow installed-inventory scenario and capability support to require `zpp-generic-workflow` in deterministic order while retaining the explicit legacy entry, then prove the focused RED.
- [ ] 1.2 Add focused artifact and lifecycle inventory cases for the six-entry public set without asserting arbitrary Markdown wording or fabricating executable routing behavior.

## 2. Workflow Entry Contracts

- [ ] 2.1 Add `zpp-generic-workflow` as the complete current generic sequence by moving the existing generic playbook without changing its ordered stages or authority boundaries.
- [ ] 2.2 Narrow `zpp-auto` so workflow-shaped mixed, maintenance-oriented, or unspecialized product requests select the generic entry, accepted direct artifact maintenance remains separate, and genuine non-matches return without handoff.
- [ ] 2.3 Reduce `zpp-legacy-workflow` to an explicit one-hop compatibility entry that preserves the request and supplied authority, invokes the generic entry once, and owns no lifecycle stages or continuation.

## 3. Projection and Lifecycle

- [ ] 3.1 Add the generic entry to the deterministic packaged workflow family and update exact inventory validation and projection ordering.
- [ ] 3.2 Verify initialization, synchronization, grouped workflow lifecycle, and reset consume the shared six-entry inventory while preserving ownership-safe obsolete handling.

## 4. Contract and Repository Verification

- [ ] 4.1 Form the spec-only routing obligations and the installed-inventory trace, then synchronize the consolidated-workflow delta into the canonical specification.
- [ ] 4.2 Run focused and complete Behave and pytest surfaces, lint, format checking, lock validation, build, supported-interpreter evidence, and strict OpenSpec validation.
