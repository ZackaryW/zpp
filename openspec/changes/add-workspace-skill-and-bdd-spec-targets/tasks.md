## 1. Workspace-management companion

- [ ] 1.1 Initialize `zpp-workspace-management` under the packaged companion role with skill-creator and no unnecessary resource directories.
- [ ] 1.2 Author the concise manual workflow, exact state-root and authority boundaries, and a focused reference for installed cross-repository command selection.
- [ ] 1.3 Generate matching `agents/openai.yaml` metadata and validate the complete skill folder.

## 2. Workflow and maintenance governance

- [ ] 2.1 Remove OpenLease terminology and command ownership from the general `zpp-workflow` artifact, delegate workspace operations and retained-successor handling to `zpp-workspace-management`, and retain every existing workflow authority gate.
- [ ] 2.2 Add exact BDD-target scenario formation rules to `zpp-workflow`, including capability ownership, requirement traceability, scenario-selected public bindings, passing verification, and preservation of every non-BDD scenario.
- [ ] 2.3 Add the same evidence-backed BDD-target reconciliation and archive-preservation rules to `zpp-maintain-openspec` and its maintenance contract.

## 3. Integration evidence

- [ ] 3.1 Verify the discovered companion inventory and packaged wheel include `zpp-workspace-management` through the existing public artifact-loading boundary.
- [ ] 3.2 Run the established companion-inventory behavior scenario and relevant artifact tests, updating only exact inventory expectations that the new packaged skill legitimately changes.
- [ ] 3.3 Validate all changed skills, validate the OpenSpec change strictly, and confirm the general workflow artifact contains no OpenLease mention.
