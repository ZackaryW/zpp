## 1. Executable audit behavior

- [x] 1.1 Add capability-owned Behave scenarios for complete-workflow discovery, one distinct subagent assignment per workflow, isolated simulation state, typed conformance findings, and targeted interactive reruns.
- [x] 1.2 Add shared Python lifecycle support in the capability support package and bindings in the established steps module without embedding reusable lifecycle logic in feature text.
- [x] 1.3 Add trace-only OpenSpec anchors for every executable workflow-auditing scenario.

## 2. Packaged audit skill

- [ ] 2.1 Add focused RED artifact tests requiring the `zpp-audit-workflows` companion skill, concise discriminating metadata, automatic discoverability, and its referenced audit contract.
- [ ] 2.2 Author `zpp-audit-workflows/SKILL.md` with complete-workflow JSON discovery, distinct queued subagent assignments, temporary product-home simulation, pre/post contamination checks, interactive review, and no-repair boundaries.
- [ ] 2.3 Add `references/audit-contract.md` with the per-workflow evidence matrix, simulation cases, typed finding schema, and aggregate result contract.
- [ ] 2.4 Add consistent `agents/openai.yaml` metadata and validate the completed skill with the skill-creator quick validator.

## 3. Projection and conformance verification

- [ ] 3.1 Update focused companion inventory and lifecycle expectations so Agent Router projects the new skill in deterministic companion order.
- [ ] 3.2 Run targeted workflow-auditing Behave scenarios and focused artifact, lifecycle, and integration pytest selectors.
- [ ] 3.3 Forward-test the installed audit skill with one fresh read-only subagent per complete workflow, queuing assignments when concurrency is exhausted.
- [ ] 3.4 Confirm forward simulations leave Git, live reminder state, and retained lease state unchanged and classify every blocked or unexecuted check truthfully.

## 4. Repository verification

- [ ] 4.1 Run formatting, lint/type checks, the complete Python test suite, and the repository's clean package build.
- [ ] 4.2 Run complete Behave verification, strict OpenSpec validation, and semantic verification for `add-workflow-audit-skill`.
