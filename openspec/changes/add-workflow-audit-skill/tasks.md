## 1. Executable audit behavior

- [x] 1.1 Add capability-owned Behave scenarios for disposable Git/OpenSpec initialization, one distinct subagent assignment per workflow, complete mock sequence closeout, typed gap ledgers, and fresh interactive reruns.
- [x] 1.2 Add shared Python lifecycle support in the capability support package and bindings in the established steps module without embedding reusable lifecycle logic in feature text.
- [x] 1.3 Add trace-only OpenSpec anchors for every executable workflow-auditing scenario.

## 2. Packaged audit skill

- [x] 2.1 Add focused RED artifact tests requiring the `zpp-audit-workflows` companion skill, concise discriminating metadata, automatic discoverability, and its referenced audit contract.
- [x] 2.2 Author `zpp-audit-workflows/SKILL.md` with complete-workflow JSON discovery, distinct queued subagents, empty Git/OpenSpec workspaces, complete mock sequence execution, contamination checks, and interactive gap closeout.
- [x] 2.3 Add `references/audit-contract.md` with workflow-specific synthetic requests, full stage/branch simulation, typed gap and closeout schemas, and aggregate result contract.
- [x] 2.4 Add consistent `agents/openai.yaml` metadata and validate the completed skill with the skill-creator quick validator.

## 3. Serial feedback-loop revision

- [ ] 3.1 Replace parallel assignment behavior with exactly one active workflow subagent, an immediate result/status checkpoint, and same-workflow rerun before advancement.
- [ ] 3.2 Add a deterministic coordinator-owned mock-base bootstrap helper and clone a fresh isolated repository from it for every initial run and rerun.
- [ ] 3.3 Reshape workflow-auditing BDD so it proves base reuse, serial assignment, observed branch events, contamination sentinels, and full-phase feedback re-entry.
- [ ] 3.4 Update the packaged skill and audit contract to make the serial checkpoint and reusable base authoritative, then validate the skill package.

## 4. Projection and conformance verification

- [x] 4.1 Update focused companion inventory and lifecycle expectations so Agent Router projects the new skill in deterministic companion order.
- [x] 4.2 Run the earlier targeted workflow-auditing Behave scenarios and focused artifact, lifecycle, and integration pytest selectors.
- [ ] 4.3 Forward-test the revised skill serially: run one workflow from a fresh base clone, review its result and sentinels, and stop before assigning the next workflow.
- [ ] 4.4 Confirm the serial simulation closes its synthetic change, leaves source Git, live reminder state, and retained lease state unchanged, and classifies every transition truthfully.
- [ ] 4.5 Interactively close every accepted source gap through the applicable full phases and rerun that workflow from a fresh base clone until it closes or retains an explicit deferred, rejected, or blocked result.

## 5. Repository verification

- [ ] 5.1 Run formatting, lint/type checks, the complete Python test suite, and the repository's clean package build.
- [ ] 5.2 Run complete Behave verification, strict OpenSpec validation, and semantic verification for `add-workflow-audit-skill`.
