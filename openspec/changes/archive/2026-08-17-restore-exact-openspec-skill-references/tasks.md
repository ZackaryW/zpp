## 1. Packaged workflow contract

- [x] 1.1 Update the component-delegation instructions in packaged `zpp-workflow` to name all six installed OpenSpec operation skills and their owned operations.
- [x] 1.2 Preserve `zpp-workflow` as the only ZPP workflow authority and avoid presenting component operation skills as stage aliases.
- [x] 1.3 Require a visible eligibility assessment before each stage action, including the current contract revision, requested stage, predecessor outcomes, blockers, and stage-owned output.
- [x] 1.4 Require conditional-stage decisions by accepted observable and executable effects rather than artifact labels, and explicitly distinguish governed prose from executable artifact update behavior.
- [x] 1.5 Add `references/stage-assessment.md` inside the packaged `zpp-workflow` skill for the detailed assessment schema, predecessor-gate matrix, effect classification, and stage-applicability evidence.
- [x] 1.6 Keep the mandatory assessment and non-bypass invariant in `SKILL.md`, require application of the complete internal reference, and add no second installed workflow or companion skill.

## 2. Specification reconciliation

- [x] 2.1 Reconcile the canonical `Explicit component delegation` requirement with the accepted exact skill inventory and operation boundaries.
- [x] 2.2 Reconcile the canonical stage-eligibility and effect-based applicability requirements with the packaged workflow instructions.
- [x] 2.3 Confirm the packaged workflow and canonical provisioning inventories name the same six OpenSpec skills.

## 3. Verification and finalization

- [x] 3.1 Record evidence-backed stage outcomes for this governed-prose change without creating Gherkin, BDD execution obligations, a utility plan, unit TDD, or wiring.
- [x] 3.2 Run strict validation for the OpenSpec change and inspect the artifact diff.
- [x] 3.3 Run the declared lock, supported-interpreter, lint, format, regression-test, and clean-build gates required for finalization without adding prose-pinning tests.
- [x] 3.4 Archive the completed OpenSpec change after canonical synchronization, without creating a commit unless separately authorized.
