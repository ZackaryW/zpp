## 1. Public behavior

- [x] 1.1 Shape initialization summary, JSON, normal idempotence, and forced reprojection scenarios in the OpenSpec provisioning capability
- [x] 1.2 Shape concise default and explicit JSON reporting scenarios in the product-home lifecycle capability

## 2. Utility behavior

- [x] 2.1 Prove deterministic one-line lifecycle summary aggregation fail-first
- [x] 2.2 Prove forced initialization routes every selected integration through ownership-aware reprojection fail-first

## 3. Product wiring

- [x] 3.1 Add default human summaries and explicit `--json` to initialization and reset
- [x] 3.2 Add `zpp init --force` and wire complete selected-agent reprojection through Agent Router
- [x] 3.3 Keep existing deterministic JSON report shapes and normal initialization idempotence

## 4. Finalization

- [x] 4.1 Run capability-owned BDD roots, focused unit tests, lint, full tests, package build, and strict OpenSpec validation
- [x] 4.2 Form canonical specifications, archive the completed change, and create the authorized logical commit
