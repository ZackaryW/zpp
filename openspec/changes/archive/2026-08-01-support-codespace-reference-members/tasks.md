## 1. Mixed-Access Locking

- [x] 1.1 Wire explicit writable and read-only targets through lock acquisition, identity, private registries, and durable migration.
- [x] 1.2 Expose complete access-aware status and projection views while keeping ownership discovery writable-only.

## 2. Atomic Codespace Editing

- [x] 2.1 Replace the add command with the complete normalized edit surface and its two-confirmation or `--yes` authority contract.
- [x] 2.2 Compose successor planning, worktree and projection staging, atomic identity transition, rollback, and released-debt preservation end to end.

## 3. Cooperative Guarding

- [x] 3.1 Propagate explicit codespace association through activate and exec environments and reject supported writes into associated read-only members.

## 4. Executable Product Contract

- [x] 4.1 Bind every approved mixed-access and edit scenario through public CLI behavior without duplicating utility logic in step definitions.
- [x] 4.2 Run the complete unit and UTF-8 Behave suites and reconcile any implementation drift before specification formation.
