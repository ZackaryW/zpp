## 1. Replacement feature contract

- [x] 1.1 Replace affected BDD feature scenarios for automatic hooks, activation modes, prompt-ready resolution, workflow-skill boundaries, and Agent Router lifecycle
- [x] 1.2 Checkpoint the complete replacement feature contract before implementation

## 2. Trait activation and rendering

- [x] 2.1 Add strict family activation metadata with automatic default and highest-precedence composition
- [x] 2.2 Implement unfiltered automatic/manual/always-run candidate behavior while preserving family selection policies
- [x] 2.3 Implement repeatable ordered direct-family filtering with unknown-family diagnostics
- [x] 2.4 Render prompt-ready bodies by default and structured deterministic diagnostics with `--explain`

## 3. Agent integration

- [x] 3.1 Update Agent Router dependency and construct it with real home plus selected repository project context
- [x] 3.2 Package and validate native Codex, Claude Code, Pi, and Kimi automatic resolution hooks
- [x] 3.3 Restore invoking-agent active plugin trait sources without cross-agent composition
- [x] 3.4 Coordinate skill-and-hook install, explicit project update, user maintenance, and removal through Agent Router

## 4. Workflow assets

- [x] 4.1 Remove resolver bootstrap and stored-context instructions from the consolidated workflow skill
- [x] 4.2 Remove the non-runtime reference behavior artifact while retaining executable coverage for the reauthored trait collection

## 5. Verification and finalization

- [x] 5.1 Wire all replacement BDD bindings through public behavior and make the complete suite green
- [x] 5.2 Verify strict OpenSpec validation, CLI help and lifecycle behavior, packaged assets, and a clean distribution build
- [x] 5.3 Reconcile canonical specifications and archive the completed corrective change
