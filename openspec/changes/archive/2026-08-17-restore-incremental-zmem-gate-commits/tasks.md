## 1. Packaged Workflow Contract

- [x] 1.1 Update the packaged `zpp-workflow` skill to invoke the exact installed `zmem-author-commits` operation before completing every material stage gate.
- [x] 1.2 Define material versus skipped or no-diff gates, narrow end-to-end checkpoint commit authority, failure behavior, selective annotations, and finalization-series verification in the packaged skill.
- [x] 1.3 Extend the stage-assessment reference with the required checkpoint evidence record, exact path or hunk scope, unrelated-change preservation, dependency-ordered decomposition, and resulting-SHA inspection.

## 2. Specification Reconciliation

- [x] 2.1 Reconcile the verified incremental checkpoint requirement into the canonical `consolidated-workflow-skill` specification without weakening existing stage, verification, or authority boundaries.
- [x] 2.2 Inspect the packaged workflow text, internal reference, delta specification, and canonical requirement together for complete terminology and scenario coverage.

## 3. Verification And Finalization

- [x] 3.1 Record that this governed prose-only change creates no executable public behavior, Gherkin, BDD, utility-plan, unit-TDD, or product-wiring obligation.
- [x] 3.2 Run strict OpenSpec validation, diff checks, lock validation, supported-interpreter verification, lint, format, the complete test suite, and a clean build through the declared backend.
- [x] 3.3 Under checkpoint commit authority, use `zmem-author-commits` for each material stage-owned diff, validate each proposed message, preserve unrelated work, and inspect every resulting SHA with `zmem show`.
- [x] 3.4 Archive the completed OpenSpec change, verify the retained checkpoint series, and commit only the remaining finalization-owned diff without collapsing earlier commits.
