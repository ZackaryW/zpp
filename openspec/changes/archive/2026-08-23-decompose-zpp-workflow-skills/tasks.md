## 1. Fail-First Executable Verification Contracts

- [x] 1.1 Reauthor `features/consolidated_workflow_skill/` around executable packaged-family loading, ordering, exact adapter inventory, and contextual-trait inventory through the package public API, then run that root independently to prove the relevant RED; do not encode playbook routing or sequencing, component procedure prose, kernel authority, adapter stopping policy, or other prompt policy as BDD.
- [x] 1.2 Reauthor `features/openspec_skill_provisioning/` with scenario-selected packaged-family projection, explicit onboarding exclusion, lifecycle-without-OpenSpec-process, and no-generated-skill cases, then run that root independently to prove the relevant RED; remove its obsolete-identity preservation scenario because `features/product_home_lifecycle/` is the single executable authority for that lifecycle behavior, and do not fabricate a runtime-adapter incompatibility scenario for an agent-only adapter.
- [x] 1.3 Add scenario-selected shared-inventory, owned-tombstone retirement, and unowned-obsolete preservation cases to `features/product_home_lifecycle/`, then run that root independently to prove the relevant RED.
- [x] 1.4 Add focused unit matrices only for executable pure functions: exact adapter and trait inventories, deterministic projection ordering, and tombstone classification; add route, envelope, or binding-parser matrices only if corresponding product functions actually exist, never to assert skill or specification wording.
- [x] 1.5 Reauthor `features/automatic_trait_hooks/` install and removal around the complete deterministic packaged workflow family plus `zpp-traits`, derive expectations from the package inventory API, bind both executable scenarios to trace-only canonical conformance anchors, and reconcile the `automatic-trait-hooks` canonical specification and change delta.

## 2. Packaged Workflow Family

- [x] 2.1 Replace the singular workflow artifact loader with a deterministic validated workflow-family loader while preserving existing callers until they migrate.
- [x] 2.2 Author `zpp-auto`, `zpp-new-feature`, `zpp-fix-bug`, `zpp-scaffold`, and the renamed generic `zpp-legacy-workflow` as complete ordered Markdown playbooks; interleave workflow-specific custom instruction blocks with explicitly configured `zpps-*` uses, state every condition and next step in each playbook, leave no sequence in the kernel or a hidden shared stage list, and ensure automatic continuation follows only declared branches without answering decisions or supplying missing owner, mutation, checkpoint, or archive authority.
- [x] 2.3 Extract `zpps-workflow-kernel` as a shared guard for requested-transition eligibility, mutation authority, Bundler acquisition/progression, checkpoint handling, component-result assessment, and truthful completion; give it no stage dispatcher, sequence selection, next-step return, phase procedure, or OpenSpec-operation implementation.
- [x] 2.4 Package `zpps-clarify`, `zpps-shape-bdd`, `zpps-planning-ponytail`, `zpps-mature-utilities`, `zpps-wire`, `zpps-form-specs`, and `zpps-finalize` as substantive skills whose own Markdown defines trigger, required inputs, input resolution, ordered bounded procedure, stopping boundary, result fields, and failures for playbook-configured or direct partial invocation; require a registered store UUID/change lease before mutation, return exact changed paths for kernel post-audit, and mark only supplied OpenSpec tasks fully satisfied by successful stage behavior and evidence.
- [x] 2.5 Package strict read-only `zpps-explore` plus `zpps-new-change`, `zpps-continue-change`, `zpps-ff-change`, `zpps-propose-change`, `zpps-update-change`, `zpps-apply-change`, `zpps-verify-change`, `zpps-sync-specs`, `zpps-archive-change`, and `zpps-bulk-archive-change` as substantive adapters preserving their complete upstream operation procedures, state handling, context/instruction/dependency discovery, prompts, stopping boundaries, outputs, and failure behavior under ZPP constraints; make fast-forward always scaffold a new change and return collisions for separately selected continue, make standalone propose stop after planning while an authorized playbook may consume its result, make verify-change return `repository-evidence-required` without invoking repository verification, and preserve synchronous archive sync.
- [x] 2.6 Package `zpps-verify-repository` as a substantive directly invokable evidence procedure, and complete the phase bodies for behavior-only shaping, Ponytail, RED/minimum-slice/GREEN maturation, composition, canonical authority audit, and final evidence; allow archive adapters only their preserved synchronous `zpps-sync-specs` sub-operation and keep all other lifecycle continuation in playbooks.
- [x] 2.7 Implement the canonical five-field compact-JSON binding declaration on both sides: create or change bindings only with an exact public-store-list-discovered `store:<uuid>`, while allowing read-only resolution and verification of an existing normalized repo-local `repo:<path>` locator; preserve capability identity, exact requirement heading, Git-root-relative feature path, and exact scenario title, transfer concrete executable examples during BDD shaping, replace them with trace-only OpenSpec conformance scenarios, and reject semantic duplicates, fabricated UUIDs, or orphaned authority before and after canonical synchronization.
- [x] 2.8 Review every subordinate skill for a complete bounded procedure and both invocation modes: it accepts explicit playbook configuration or direct partial configuration, does not reject solely for lack of prior kernel delegation, returns `store-registration-required` before governed mutation without an exact registered store UUID/change member, obtains a kernel guard and lease containing only that exact member, returns exact changed paths for post-result audit rather than treating paths as lease members, and rejects workflow selection, stage continuation, lease expansion, checkpoint authority, and lifecycle completion; review `zpps-verify-repository` for absence of OpenSpec-satisfaction or mutation authority, and do not turn this prompt policy into executable tests or wording assertions.

## 3. Trait Responsibility Migration

- [x] 3.1 Move Ponytail, zero-assumption reconciliation, build completeness, BDD binding and single-authority transfer, executable verification truthfulness, and shared RED/GREEN invariants into their specified owning skills.
- [x] 3.2 Remove packaged `dependencies`, `build`, and `zero-assumptions` trait documents and update exact standard-inventory expectations.
- [x] 3.3 Narrow `bdd`, `bdd-execution`, and `tdd` bodies to contextual language, framework, structure, and mode specialization without repeating or weakening skill invariants.
- [x] 3.4 Preserve and validate `bdd-structure` and `tooling` as contextual families and prove representative resolution composes them with the owning phase skills.

## 4. OpenSpec Provisioning Replacement

- [x] 4.1 Replace singular packaged-skill preparation with deterministic workflow-family validation and rewrite provisioning support around packaged adapters rather than upstream generation.
- [x] 4.2 Remove `generated_openspec_skill_sets` from `_initialize_selected` and `_synchronize`, remove their generated projection passes, and delete `generated_entries` so root initialization and synchronization invoke no OpenSpec process.
- [x] 4.3 Reauthor the eleven exact ZPP-owned adapters from the corresponding upstream OpenSpec operations, preserving each full selection, sticky registered-store or repo-local read-only resolution, structured status/instruction/context/rule/dependency handling, ordered work, prompts, stopping boundary, output, and failure procedure; require exact registered store UUID/change membership before every mutation, keep changed paths solely as kernel post-result audit evidence, and tailor only for Bundler guards, single BDD authority, no onboarding, no `openspec init` or generated skills, and no compatibility behavior while checking only required public runtime interfaces.
- [x] 4.4 Delete `zpp.utils.openspec` including `OPENSPEC_CORE_SKILL_NAMES`, `OpenSpecGenerationError`, `OpenSpecProvenance`, provenance writing, process/version detection, temporary repository generation, generated skill-set contexts, and agent-relative generated roots; delete generation-only unit tests and mocks after all live callers are gone.

## 5. Lifecycle Hard Cut

- [x] 5.1 Update the shared initialization, synchronization, reset, inspection, and reporting inventory to the complete packaged workflow family with no `zpp-workflow` or generated `openspec-*` member, and remove the reset dependency on `OPENSPEC_CORE_SKILL_NAMES`.
- [x] 5.2 Add an inert explicit obsolete identity inventory outside the removed OpenSpec generation module and remove only Agent Router-owned tombstones during synchronization and reset while preserving and reporting unowned collisions; tombstones SHALL provide no generation, repair, provenance, version, or install behavior.
- [x] 5.3 Replace repository and test behavior-gate mappings with `zpps-workflow-kernel` and verify that old gates receive deterministic affected selection rather than translation.
- [x] 5.4 Remove the packaged `zpp-workflow` directory after every loader and lifecycle caller uses the new family, and confirm no compatibility alias remains.

## 6. Complete Verification and Specification Formation

- [x] 6.1 Run focused unit tests and each of `features/consolidated_workflow_skill/`, `features/openspec_skill_provisioning/`, and `features/product_home_lifecycle/` independently, then run the complete native test surface.
- [x] 6.2 Run the supported-interpreter, lock, lint, format, and clean package-build gates through the declared build backend.
- [x] 6.3 Inspect a projected installation and run lifecycle integration coverage to prove exact current identities, deterministic order, removed trait and workflow identities, no generated OpenSpec skills, no OpenSpec process invocation by init/sync/reset, and ownership-safe obsolete cleanup.
- [x] 6.4 Reconcile the changed canonical capability purposes and requirements, including current Bundler acquisition ownership and maintenance routing through ZPP-owned adapters; add identical five-field declarations to both sides of every executable binding, replace every bound canonical WHEN/THEN example with its trace-only conformance anchor, remove cross-root BDD duplicates, strictly validate the OpenSpec change, and verify every material checkpoint before archival.

## 7. Lifecycle Migration and Distribution Overhaul

- [x] 7.1 Add scenario-selected RED Behave coverage for owned old-only user installations migrating through root `zpp init` and root `zpp sync`, an owned old-only project installation migrating through `zpp workflow update --target`, and conflict-safe grouped workflow installation; bind each scenario only to its exact trace-only OpenSpec authority, keep bindings thin, and run each capability root independently.
- [x] 7.2 Add pure-function unit matrices for current-plus-obsolete installation classification, exact scope propagation, finite tombstone selection, ownership-safe retirement eligibility, ordered current-family-before-obsolete actions, and truthful complete/partial/conflict result aggregation.
- [x] 7.3 Refactor lifecycle inventory construction and reconciliation into one shared operation accepting exact agent, `Scope`, and optional project root, inspecting both the deterministic current inventory and exact finite obsolete inventory without a prefix, glob, inferred identity, or direct native-destination mutation.
- [x] 7.4 Update root `zpp init` to initialize an empty user integration, migrate an owned old-only user integration, reject any installation already carrying a current entry to `zpp sync`, and preserve an unowned obsolete conflict without creating a mixed installation.
- [x] 7.5 Update root `zpp sync` to migrate owned old-only user integrations and grouped `zpp workflow update` to reconcile owned old-only integrations in exactly its selected user or project scope; keep package installation and upgrade non-mutating until one of these explicit commands runs.
- [x] 7.6 Make grouped `zpp workflow install` preflight all current and obsolete destinations before mutation, reject an installed, unmanaged, or conflicting destination without projecting a partial family, report the exact conflict, and direct an owned existing installation to update.
- [x] 7.7 Install and verify the complete current family before removing any owned obsolete entry; preserve all obsolete entries on current-family failure, and report exact surviving obsolete identities as a partial migration when retirement fails after current verification.
- [x] 7.8 Revise `zpp-auto` so successful triage invokes its selected complete `zpp-*` playbook exactly once with the original request, accepted classification evidence, and only supplied authority, then transfers control rather than merely reporting the route and stopping; verify this as skill/spec prompt policy, not an artifact-wording test.
- [x] 7.9 Align project distribution metadata, `zpp.__version__`, CLI version output, and built wheel metadata to one release version; keep OpenSpec Bundler as a library dependency with no executable and no separate tool-install instruction.
- [x] 7.10 Build the ZPP wheel and exercise it in a disposable `uv` tool environment, proving `zpp` is the sole installed tool command, its version matches the distribution, and OpenSpec Bundler is present only as a dependency; add the scenario-selected public integration binding without asserting package-file prose.
- [x] 7.11 Run the two changed capability-owned BDD roots independently, focused lifecycle and packaging tests, the complete native test surface, supported-interpreter and lock checks, lint, format, and a clean build; reconcile canonical specifications and strictly validate the change before archival.
