## 1. Executable Feature Contract

- [x] 1.1 Define global complete-install scenarios for ZPP skills, current native hooks, agent-specific OpenSpec core skills, atomic conflicts, and idempotency.
- [x] 1.2 Define optional local OpenSpec-bootstrap scenarios and Pi's independent local and global `.pi` destinations.
- [x] 1.3 Define update and removal scenarios for recorded OpenSpec versions, unchanged-byte preservation, regeneration, and retained shared integrations.
- [x] 1.4 Define exact legacy-hook migration and unmanaged-conflict preservation scenarios.

## 2. Proven Utility Foundation

- [x] 2.1 Implement and verify isolated platform-neutral OpenSpec core-skill generation with version detection and guaranteed temporary-project cleanup.
- [x] 2.2 Implement and verify generated-projection validation, manifest ownership, nullable version comparison, and exact-content conflict detection.
- [x] 2.3 Extend agent projection resolution for Codex, Claude Code, and Pi's distinct local and global `.pi` roots.
- [x] 2.4 Extend native-hook reconciliation to migrate only exact historical ZPP records.
- [x] 2.5 Compose complete selected-agent lifecycle planning so every hook and skill surface is preflighted before mutation and rollback remains atomic.

## 3. Workflow Wiring

- [x] 3.1 Wire global workflow installation to complete ZPP, hook, and OpenSpec projections for every selected agent.
- [x] 3.2 Wire repository-local OpenSpec projection behind an explicit opt-in while retaining default ZPP-only local installation.
- [x] 3.3 Wire workflow update to reconcile hooks and regenerate OpenSpec projections only after a recorded version change.
- [x] 3.4 Preserve native hooks and OpenSpec projections during workflow removal while removing only owned ZPP workflow paths.
- [x] 3.5 Emit clear lifecycle results and atomic failure diagnostics without exposing temporary implementation paths as product state.

## 4. Verification and Finalization

- [x] 4.1 Run focused utility and CLI tests, the complete BDD feature set, and the full pytest suite.
- [x] 4.2 Reconcile mature green behavior into the two canonical capability specifications.
- [x] 4.3 Finalize the related product change and checkpoint its exact archive without sweeping unrelated work.
