## 1. Executable behavior contract

- [x] 1.1 Add Gherkin coverage for behavior mapping initialization, strict validation, local and revision impact selection, unknown-path fallback, clean no-op, and `--all` selection.
- [x] 1.2 Add Gherkin coverage for safe argv expansion, Nx discovery and surface validation, provider unavailability, opaque repository plugins, and separate audit commands.
- [x] 1.3 Extend workflow and bootstrap feature coverage for the twelfth configuration skill, inactive ecosystem BDD traits, additive global default upgrades, and local/removal isolation.

## 2. BDD structure foundation

- [x] 2.1 Move the Behave suite into `core`, `workflow`, and `codespaces` execution roots with root-owned modular bindings and shared non-step support.
- [x] 2.2 Add structural tests that enforce stable root ownership, small step modules, and no decorated steps in shared support.
- [x] 2.3 Run every restructured execution root independently and together before building behavior orchestration.

## 3. Verification utilities

- [x] 3.1 Implement and unit-test strict duplicate-safe `zpp.behave.yaml` loading, typed models, scaffold generation, target ordering, and impact-glob validation.
- [x] 3.2 Implement and unit-test Git worktree discovery plus local and explicit-revision changed-path collection.
- [x] 3.3 Implement and unit-test deterministic affected-target selection, unknown-path all-target fallback, and typed in-place argv expansion.
- [x] 3.4 Implement and unit-test repository-local/PATH Nx discovery, project/target surface validation, and shell-free Nx argv construction without downloads or plugin management.

## 4. Core and CLI wiring

- [x] 4.1 Add the `behave` Typer group with `init`, named command, `--all`, `--base`, and `--head` behavior and stable diagnostics/exit propagation.
- [x] 4.2 Compose mapping, change selection, and argv/Nx providers through the process boundary, including clean no-op and unavailable-provider failures.
- [x] 4.3 Add this repository's committed provider-neutral `zpp.behave.yaml` with filtered `bdd` and separately declared `bdd-audit` commands.

## 5. Packaged guidance

- [x] 5.1 Package inactive `bdd-structure-python`, `bdd-structure-ts`, and `bdd-structure-flutter` traits with ecosystem-native guidance and no Nx trigger.

## 6. Workflow bundle and global profile maintenance

- [x] 6.1 Package `zpp-configure-behavior`, update bundle ownership and tests from eleven to twelve skills, and make its runtime authority boundary explicit.
- [x] 6.2 Update `zpp-wire-feature` to prefer a configured complete behavior command while retaining the established-runner fallback.
- [x] 6.3 Implement and unit-test compatible additive persistent-default planning for global workflow install/update, including malformed-state atomic failure, idempotence, and preservation of authored content.
- [x] 6.4 Compose the default upgrade into global workflow lifecycle transactions without changing local workflow operations or removal.

## 7. Integrated verification and rollout

- [x] 7.1 Run focused unit tests, each capability-oriented Behave root, mapped filtered execution, and the uncached complete audit; resolve every regression.
- [x] 7.2 Validate and form the canonical OpenSpec specifications, archive the completed change, and leave no consumed related active change.
- [x] 7.3 Install/update the completed workflow globally for Pi, Codex, and Claude Code, verify their native hooks and twelve-skill projections, and validate the globally upgraded default profile.
