## 1. Public Contract

- [x] 1.1 Replace every workflow lifecycle invocation in the complete Gherkin set with `zpp workflow`.
- [x] 1.2 Require root help to expose `workflow` and omit the generic `skill` group.
- [x] 1.3 Create the replacement feature-contract checkpoint after repairing the complete breaking-change contract.

## 2. Utility Gate

- [x] 2.1 Inspect the existing lifecycle utility graph and dependency set.
- [x] 2.2 Confirm that the command-boundary rename requires no utility or dependency change.
- [x] 2.3 Discard the disposable utility plan without creating a no-change zmem checkpoint.

## 3. Wiring and Verification

- [x] 3.1 Register the Typer group and CLI-local lifecycle helpers under `workflow`.
- [x] 3.2 Bind every affected feature step to the renamed public path.
- [x] 3.3 Verify the focused feature set, complete unit suite, complete Behave suite, root help, removed command, and built wheel.
- [x] 3.4 Create the material green-integration checkpoint.

## 4. Specification Formation

- [x] 4.1 Form complete delta requirements for the two modified capabilities.
- [x] 4.2 Record the implemented design and completed task evidence without duplicating Gherkin examples.
