## 1. Evidence authority

- [x] 1.1 Update BDD shaping to reject literal-text-only, self-recording, execution-only, and pure-counting evidence while allowing counts only as supplemental behavioral constraints.
- [x] 1.2 Update shaping, specification formation, synchronization, semantic verification, archive, and OpenSpec maintenance guidance so an exact BDD feature scenario removes the corresponding OpenSpec scenario rather than creating a trace-only replacement.
- [x] 1.3 Add focused verification for feature-side requirement bindings, absent BDD-owned OpenSpec scenarios, preserved non-BDD scenarios, and rejected weak evidence.

## 2. Active-change checkpoint boundary

- [x] 2.1 Update every complete playbook and the workflow kernel to retain incremental source/test commits while excluding active OpenSpec change-root paths from staging.
- [x] 2.2 Update zmem commit guidance to require an active-change path audit before message validation and commit creation.
- [x] 2.3 Add focused tests proving tasks remain updated in the working tree, active change artifacts remain uncommitted, and coherent source checkpoints still succeed.

## 3. Terminal preservation choice

- [x] 3.1 Add conservative normal-archive versus memory-fold classification to finalization, including explicit owner authority and non-foldable contract categories.
- [x] 3.2 Add recoverable memory-fold execution to the single-change terminal adapter: validate zmem, move the active change outside the worktree, commit and inspect, restore on failure, and create no archive path.
- [x] 3.3 Update playbook branches, kernel lifecycle assessment, component result vocabularies, and coordination release behavior for `memory-fold-required` and `memory-folded` results.
- [x] 3.4 Add focused tests for eligible simple folds, rejected nested/normative changes, successful no-archive completion, and failure restoration.

## 4. Canonical authority and verification

- [x] 4.1 Synchronize the accepted consolidated-workflow and OpenSpec-maintenance delta requirements without retaining BDD-owned OpenSpec scenarios.
- [x] 4.2 Run focused skill, contract, lifecycle, and OpenSpec validation targets.
- [x] 4.3 Run the complete Python and Behave suites, lint, format, clean build, and semantic change verification.
- [x] 4.4 Keep this task list current without committing the active change directory; normally archive this non-foldable policy change before committing its terminal planning evidence.
