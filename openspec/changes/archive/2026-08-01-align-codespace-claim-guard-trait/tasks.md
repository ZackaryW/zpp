## 1. Packaged claim-guard conformance

- [x] 1.1 Make the resolved packaged default trait explicitly reject supported direct writes into associated read-only codespace members, with executable BDD coverage.

## 2. Installed user-space alignment

- [x] 2.1 Additively align the current user-owned default, archive and reactivate it into global, and reinstall the implementation checkout as the global ZPP executable.

## 3. End-to-end verification

- [x] 3.1 Verify the focused trait contract, full utility and BDD suites, global trait resolution, and global codespace schema compatibility before finalizing the change.
