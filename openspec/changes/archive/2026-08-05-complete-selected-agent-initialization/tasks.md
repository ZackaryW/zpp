## 1. Public Contract

- [x] 1.1 Revise bootstrap feature scenarios so explicit and interactive agent selections require complete global ZPP, hook, and OpenSpec integration while no-selection outcomes remain skill-free.
- [x] 1.2 Add scenarios for the observed Codex state, multi-agent atomic conflicts, outdated managed projections, compatible idempotence, and preservation of an existing authored default profile.

## 2. Complete Integration Planning

- [x] 2.1 Add fail-first focused tests for reusable complete global integration planning with caller-controlled persistent-default mutation.
- [x] 2.2 Extract the selected-agent global workflow, native-hook, and OpenSpec mutation composition from direct workflow-command application.
- [x] 2.3 Preserve global workflow install's additive default-profile upgrade while allowing initialization to keep an existing valid default byte-for-byte unchanged.
- [x] 2.4 Verify absent, compatible, outdated, malformed, modified, and unmanaged projection classifications remain ownership-safe.

## 3. Initialization Wiring

- [x] 3.1 Route explicit and interactive initialization selections through the complete global integration planner after neutral user-state initialization.
- [x] 3.2 Preserve empty, cancelled, unsupported, and noninteractive no-selection behavior without invoking skill or OpenSpec planning.
- [x] 3.3 Update initialization help and README setup guidance to describe complete selected-agent setup.

## 4. Verification

- [x] 4.1 Run focused initialization, skill lifecycle, OpenSpec generation, projection, and transaction unit tests.
- [x] 4.2 Run the complete bootstrap and workflow BDD feature roots plus the configured mapped verification audit.
- [x] 4.3 Run the full test suite, strict OpenSpec validation, and final diff review.
