## 1. Public Contract

- [x] 1.1 Add a workflow feature scenario proving Codex global and local installations use `.codex/skills` and `.agents/skills` respectively.
- [x] 1.2 Cover coexistence of the global ZPP workflow and OpenSpec manifests plus preservation of unrelated content.

## 2. Projection Adapter

- [x] 2.1 Add fail-first focused projection tests for scope-specific Codex roots, supplied platform-native homes, and shared global ownership.
- [x] 2.2 Make the Codex workflow projection root scope-aware without changing Pi or Claude mappings.
- [x] 2.3 Verify global install, update, remove, and top-level update all consume the corrected mapping.

## 3. Documentation and Verification

- [x] 3.1 Correct README and canonical path wording while retaining repository-local `.agents/skills`.
- [x] 3.2 Run focused projection/lifecycle tests, the complete workflow feature root, mapped BDD audit, full pytest, and strict OpenSpec validation.
- [x] 3.3 Reconcile the mature correction into canonical specifications and archive the completed change.

## 4. Global Installation Verification

- [x] 4.1 Install the corrected package into the user's global ZPP tool environment.
- [x] 4.2 Run explicit Codex global workflow install and verify the managed bundle and OpenSpec projection coexist under `~/.codex/skills` without changing historical `~/.agents/skills` content.
