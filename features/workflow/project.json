{
  "name": "zpp-workflow-bdd",
  "root": "features/workflow",
  "projectType": "application",
  "targets": {
    "bdd": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": ["bddShared", "{projectRoot}/**/*"],
      "outputs": [],
      "options": {
        "command": "uv run python scripts/run_behave_roots.py features/workflow --format progress",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
